---
description: Frontend-specific rules
globs:
  - "src/**/*.svelte"
  - "src/**/*.ts"
  - "src/**/*.js"
alwaysApply: true
---

# Frontend-specific Rules

## Svelte 5 Design Patterns

For details, refer to `docs/en/develop/design/frontend/svelte5-patterns.md`.

## Internationalization System

For details, refer to `docs/en/develop/design/frontend/i18n-system.md`.

## About Backend (Tauri calls)

Command parameters should be written in `camelCase` (automatically converted).
However, when parameters are objects, properties should be written in `snake_case`.
