# アーキテクチャ分析とリファクタリング提案

**分析日**: 2025-11-23
**分析対象**: Flequit タスク管理アプリケーション
**総合評価**: ⭐⭐⭐⭐☆ (4.2/5)

## 📊 エグゼクティブサマリー

**結論**: バックエンドは完璧なレイヤリングが実装されているが、フロントエンドは機能に対して **1.5〜2倍複雑**。UIコンポーネントとストアの過度な細分化が主な課題。

### 主要な問題点

1. **フロントエンド: UIコンポーネントの過度な細分化** (最重要)
   - `components/ui/` に158個のファイル → 30個程度に削減可能

2. **フロントエンド: ストアの責務が不明確**
   - Task関連だけで10ファイル以上に分散
   - `stores/` と `services/domain/` で責務重複

3. **バックエンド: flequit-infrastructureの責務過多**
   - 統合層と実装層の両方を担当

### 推定される改善効果

優先度の高い改善（1-3）を実施することで、コードベースの複雑さを **30-40% 削減**できると推定。

---

## 🏗️ アーキテクチャ概要

### バックエンド構造 (⭐⭐⭐⭐⭐ 完璧)

#### クレート構成 (9クレート、合計38,596行)

| クレート | 行数 | 責務 | 評価 |
|---------|------|------|------|
| **flequit-infrastructure-automerge** | 10,391 | Automerge実装層（永続化・同期） | ✅ |
| **flequit-infrastructure-sqlite** | 9,999 | SQLite実装層（読み取り最適化） | ✅ |
| **flequit-infrastructure** | 6,040 | インフラ統合層 | ⚠️ 責務過多 |
| **flequit-core** | 4,851 | ビジネスロジック層 | ⚠️ やや肥大化 |
| **flequit-model** | 3,920 | ドメインモデル層 | ✅ |
| **flequit-repository** | 1,458 | リポジトリインターフェース層 | ✅ |
| **flequit-settings** | 753 | 設定管理層 | ✅ |
| **flequit-testing** | 108 | テストユーティリティ | ✅ |
| **flequit-types** | 76 | 共通型・エラー定義 | ✅ |

#### レイヤリング構造

```
Layer 6: Tauri Commands (src/commands/*.rs - 14ファイル)
         ↓
Layer 5: flequit-core (services/ + facades/ - 25モジュール)
         ↓
Layer 4: flequit-infrastructure (unified repositories - 20個)
         ↓
Layer 3: flequit-infrastructure-{sqlite,automerge} (実装層)
         ↓
Layer 2: flequit-repository (interface - 35トレイト)
         ↓
Layer 1: flequit-model + flequit-types (ドメインモデル)
```

**特徴**:
- ✅ 完全な一方向依存（循環依存なし）
- ✅ オニオンアーキテクチャの完璧な実装
- ✅ SQLite/Automerge の並列実装と統一インターフェース

### フロントエンド構造 (⭐⭐⭐☆☆ 改善の余地あり)

#### ディレクトリ構成 (517ファイル、合計32,141行)

| ディレクトリ | ファイル数 | 行数 | 割合 | 評価 |
|------------|----------|------|------|------|
| **components/** | 298 | 16,497 | 51.3% | ⚠️ 過度に細分化 |
| └ ui/ | 158 | - | - | ⚠️ 最大の問題 |
| **stores/** | 52 | 4,921 | 15.3% | ⚠️ 責務重複 |
| **services/** | 56 | 5,039 | 15.7% | ⚠️ stores/と重複 |
| **infrastructure/** | 59 | 3,379 | 10.5% | ✅ 良好 |
| **types/** | 18 | 1,701 | 5.3% | ✅ |
| **utils/** | 11 | 1,406 | 4.4% | ✅ |
| その他 | 23 | - | - | ✅ |

#### レイヤリング構造

```
Layer 4: Routes (+page.svelte) + Components
         ↓
Layer 3: services/domain/ + stores/ (責務重複 ⚠️)
         ↓
Layer 2: infrastructure/backends/ (Tauri/Web実装)
         ↓
Layer 1: types/ + utils/
```

**特徴**:
- ⚠️ Layer 3-4 の境界が曖昧
- ⚠️ Layer 3 で責務の重複
- ✅ インフラ抽象化は完璧（Tauri/Web切り替え可能）

---

## 🔍 詳細な問題分析

### 🔴 高優先度の問題

#### 1. components/ui/ の過度な細分化 (最重要課題)

**現状**:
```
components/ui/ (158ファイル)
├── alert-dialog/ (4ファイル)
├── avatar/ (2ファイル)
├── badge/ (2ファイル)
├── button/ (3ファイル)
├── calendar/ (5ファイル)
├── card/ (4ファイル)
├── checkbox/ (2ファイル)
├── command/ (6ファイル)
├── dialog/ (6ファイル)
├── dropdown-menu/ (8ファイル)
├── input/ (2ファイル)
├── label/ (2ファイル)
├── popover/ (3ファイル)
├── scroll-area/ (4ファイル)
├── select/ (7ファイル)
├── separator/ (2ファイル)
├── switch/ (2ファイル)
├── tabs/ (4ファイル)
├── textarea/ (2ファイル)
├── tooltip/ (3ファイル)
└── ... (その他多数)
```

**問題点**:
- 1-2個のコンポーネントのみ定義されたファイルが多数
- shadcn/ui などのジェネレータ出力物をそのまま使用している可能性
- 保守コストが高い（修正時に複数ファイルを跨ぐ）

**改善案**:
- 関連コンポーネントを同一ファイルに統合
- 使用頻度の低いコンポーネントの削除検討
- 目標: **158ファイル → 30ファイル程度**

**優先度**: 🔴 **最高** (保守性への影響大)

---

#### 2. stores/ と services/domain/ の責務重複

**現状 - Task関連の例**:

```typescript
// stores/ (52ファイル)
stores/
├── tasks.svelte.ts                        // ファサード
├── tasks/
│   ├── task-entities-store.svelte.ts      // エンティティ管理
│   ├── task-selection-store.svelte.ts     // 選択状態
│   ├── task-draft-store.svelte.ts         // ドラフト
│   └── task-tag-operations.svelte.ts      // タグ操作
├── task-core-store.svelte.ts              // コア機能？
├── task-core/
│   ├── task-core-operations.svelte.ts     // 操作
│   ├── task-core-queries.svelte.ts        // クエリ
│   └── task-core-mutations.svelte.ts      // ミューテーション
├── task-list-store.svelte.ts              // リスト表示
├── task-list/
│   └── ... (さらに細分化)
└── task-detail-view-store.svelte.ts       // 詳細表示

// services/domain/ (56ファイル)
services/domain/
├── task/
│   ├── task-crud.ts                       // CRUD操作
│   ├── mutations/
│   │   ├── task-crud-mutations.ts         // CRUD
│   │   ├── task-move-mutations.ts         // 移動
│   │   ├── task-status-mutations.ts       // ステータス
│   │   └── task-tag-mutations.ts          // タグ
│   └── queries/
│       └── ...
└── ...
```

**問題点**:
- `stores/` と `services/domain/` でCRUD操作が重複実装
- `task-core` vs `task-list` vs `tasks` の関係が不明確
- Svelte 5のリアクティブシステムとの整合性が曖昧

**改善案**:

**オプション A (推奨)**: ビジネスロジックを `stores/` に統合
```typescript
stores/
├── tasks.svelte.ts              // ファサード（公開API）
└── tasks/
    ├── entities.svelte.ts       // エンティティ管理 + CRUD
    ├── selection.svelte.ts      // 選択状態
    ├── draft.svelte.ts          // ドラフト
    └── ui-state.svelte.ts       // UI状態（リスト/詳細）
```
- `services/domain/` を削除
- ビジネスロジックは `stores/` 内部で完結

**オプション B**: `services/domain/` をユースケース層に格上げ
```typescript
services/application/
├── use-cases/
│   ├── create-task.ts
│   ├── update-task-status.ts
│   └── move-task-to-project.ts
└── ...

stores/
├── tasks.svelte.ts              // 状態管理のみ
└── ...
```
- `services/domain/` を `services/application/use-cases/` にリネーム
- `stores/` は純粋な状態管理のみ

**優先度**: 🔴 **最高** (責務の明確化が必要)

---

#### 3. ストア設計のドキュメント不足

**問題点**:
- `task-core` と `task-list` と `tasks` の違いが不明
- Facade パターンの使い方が統一されていない
- 新規開発者が構造を理解するのに時間がかかる

**改善案**:
- `docs/en/develop/design/frontend/store-architecture.md` を作成
- 各ストアの責務と関係性を図示
- Facade パターンの使用ガイドライン

**優先度**: 🔴 **高** (保守性・オンボーディング)

---

### 🟡 中優先度の問題

#### 4. flequit-infrastructure の責務過多

**現状**:
```
flequit-infrastructure/
├── config.rs                          // 設定管理
├── infrastructure_repositories.rs      // 統合管理
└── unified/                           // 統一リポジトリ実装（20個）
    ├── task_projects/
    │   ├── project.rs
    │   ├── task.rs
    │   ├── tag.rs
    │   └── ...
    └── ...
```

**問題点**:
- SQLite/Automerge の統合層と実装層の両方を担当
- 単一責任原則に違反
- 6,040行とやや肥大化

**改善案**:

**新しいクレート構成**:
```
flequit-infrastructure-unified (新規)
├── unified repositories の実装を移行
└── 統一インターフェースの提供

flequit-infrastructure (既存)
├── config.rs のみ残す
├── SQLite/Automerge の統合管理
└── ファクトリーパターンで実装を選択
```

**依存関係**:
```
flequit-core
    ↓
flequit-infrastructure (統合層のみ)
    ↓
flequit-infrastructure-unified (統一リポジトリ)
    ↓
├─ flequit-infrastructure-sqlite
└─ flequit-infrastructure-automerge
```

**優先度**: 🟡 **中** (アーキテクチャの整理)

---

#### 5. flequit-core の関連サービス統合

**現状**:
```rust
// flequit-core/src/services/ (13サービス)
- task_service.rs
- task_tag_service.rs
- task_assignment_service.rs
- task_list_service.rs
- task_recurrence_service.rs (間接的)
- project_service.rs
- tag_service.rs
...
```

**問題点**:
- Task関連のサービスが5つに分散
- 密接に関連する操作が別サービスに分離
- サービス間の調整が必要な場合にコードが複雑化

**改善案 - Aggregate Pattern の導入**:

```rust
// flequit-core/src/aggregates/task_aggregate.rs
pub struct TaskAggregate {
    task_repo: Arc<dyn TaskRepository>,
    tag_repo: Arc<dyn TagRepository>,
    assignment_repo: Arc<dyn TaskAssignmentRepository>,
    list_repo: Arc<dyn TaskListRepository>,
}

impl TaskAggregate {
    // 基本CRUD
    pub async fn create_task(&self, ...) -> Result<Task>
    pub async fn update_task(&self, ...) -> Result<Task>

    // タグ操作
    pub async fn add_tag_to_task(&self, ...) -> Result<()>
    pub async fn remove_tag_from_task(&self, ...) -> Result<()>

    // アサイン操作
    pub async fn assign_user_to_task(&self, ...) -> Result<()>

    // リスト操作
    pub async fn move_task_to_list(&self, ...) -> Result<()>
}
```

**メリット**:
- 関連操作が1箇所に集約
- トランザクション管理が容易
- テストが書きやすい

**優先度**: 🟡 **中** (保守性向上)

---

#### 6. Tauri Commands の細分化

**現状**:
```
src-tauri/src/commands/ (14ファイル)
├── account.rs
├── initialization.rs
├── project.rs
├── settings.rs
├── tag.rs
├── task.rs
├── task_assignment.rs
├── task_list.rs
├── task_recurrence.rs
├── user.rs
└── ...
```

**問題点**:
- 各コマンドが単純なファサード呼び出しのみ
- ファイル数が多く、横断的な変更が困難

**改善案**:

**オプション A**: エンティティごとに統合
```rust
// commands/task_commands.rs
mod task_commands {
    // task, task_assignment, task_list, task_recurrence を統合
}
```

**オプション B**: Command Router の導入
```rust
// commands/router.rs
pub struct CommandRouter {
    task_aggregate: Arc<TaskAggregate>,
    project_service: Arc<ProjectService>,
    ...
}

// 各コマンドは router.rs 内でメソッドとして定義
```

**優先度**: 🟡 **中〜低** (機能への影響は小さい)

---

### 🟢 低優先度の問題

#### 7. infrastructure/backends/ の実装重複

**現状**:
```typescript
infrastructure/backends/
├── tauri/ (19サービス)
│   ├── TaskTauriService.ts
│   ├── ProjectTauriService.ts
│   └── ...
└── web/ (16サービス)
    ├── TaskWebService.ts
    ├── ProjectWebService.ts
    └── ...
```

**問題点**:
- 各サービスで `backend-client` 呼び出しが重複
- 共通部分（エラーハンドリング、型変換など）が重複実装

**改善案 - Generic Service Template**:

```typescript
// infrastructure/backends/base-service.ts
abstract class BaseBackendService<T, CreateDTO, UpdateDTO> {
  protected abstract entityName: string;

  async getAll(): Promise<T[]> {
    return await this.callBackend(`${this.entityName}_get_all`);
  }

  async create(dto: CreateDTO): Promise<T> {
    return await this.callBackend(`${this.entityName}_create`, dto);
  }

  protected abstract callBackend(command: string, args?: any): Promise<any>;
}

// infrastructure/backends/tauri/task-tauri-service.ts
class TaskTauriService extends BaseBackendService<Task, CreateTaskDTO, UpdateTaskDTO> {
  protected entityName = 'task';

  protected async callBackend(command: string, args?: any) {
    return await backendClient.call(command, args);
  }
}
```

**優先度**: 🟢 **低** (DRY原則だが、現状でも動作)

---

#### 8. 型定義の共通化

**現状**:
```
バックエンド: flequit-model/src/models/*.rs
フロントエンド: src/lib/types/*.ts
```

**問題点**:
- 型定義が二重管理
- 同期が取れなくなるリスク

**改善案**:
- TypeScript型をRustの構造体から自動生成
- ツール候補: `ts-rs`, `typeshare`

```rust
// flequit-model/src/models/task.rs
#[derive(Serialize, Deserialize, TS)]
#[ts(export, export_to = "../src/lib/types/generated/")]
pub struct Task {
    pub id: TaskId,
    pub title: String,
    ...
}
```

**優先度**: 🟢 **低** (将来的な改善)

---

## 📋 リファクタリングロードマップ

### フェーズ 1: 最重要課題の解決 (1-2週間)

#### タスク 1.1: components/ui/ の統合
- **目標**: 158ファイル → 30ファイル
- **手順**:
  1. 使用頻度分析（未使用コンポーネントの特定）
  2. 関連コンポーネントのグルーピング
  3. ファイル統合（例: button 関連を1ファイルに）
  4. テスト実行（ビジュアルリグレッション）
- **影響範囲**: フロントエンド全体
- **リスク**: 中（インポートパスの変更が広範囲）

#### タスク 1.2: stores/ と services/domain/ の責務統一
- **目標**: ビジネスロジックを1箇所に集約
- **手順**:
  1. `stores/` と `services/domain/` の責務マッピング
  2. 統合戦略の決定（オプションA or B）
  3. Task関連から段階的に移行
  4. 他のエンティティに展開
- **影響範囲**: フロントエンド全体
- **リスク**: 高（大規模な変更）

#### タスク 1.3: ストア設計ドキュメント作成
- **目標**: `docs/en/develop/design/frontend/store-architecture.md`
- **内容**:
  - ストアの責務定義
  - Facade パターンの使用ガイドライン
  - 命名規則
  - 状態管理フロー図
- **影響範囲**: ドキュメントのみ
- **リスク**: 低

---

### フェーズ 2: アーキテクチャの整理 (1-2週間)

#### タスク 2.1: flequit-infrastructure の分割
- **目標**: 統合層と実装層の分離
- **手順**:
  1. `flequit-infrastructure-unified` クレート作成
  2. `unified/` ディレクトリを移行
  3. 依存関係の調整
  4. テスト実行
- **影響範囲**: バックエンド全体
- **リスク**: 中（ビルドエラーの可能性）

#### タスク 2.2: flequit-core の Aggregate Pattern 導入
- **目標**: Task関連サービスの統合
- **手順**:
  1. `TaskAggregate` の設計
  2. 既存サービスのメソッドを移行
  3. ファサードの更新
  4. 段階的に他のエンティティに展開
- **影響範囲**: バックエンド（flequit-core）
- **リスク**: 中（互換性の維持）

---

### フェーズ 3: 最適化と改善 (継続的)

#### タスク 3.1: infrastructure/backends/ の共通化
- **優先度**: 低
- **実施タイミング**: 新機能追加時に段階的に

#### タスク 3.2: 型定義の自動生成
- **優先度**: 低
- **実施タイミング**: 型の不整合が問題になった時

#### タスク 3.3: Tauri Commands の整理
- **優先度**: 低
- **実施タイミング**: コマンド数が20を超えた時

---

## 📊 期待される効果

### 定量的効果

| 指標 | 現状 | 目標 | 削減率 |
|-----|------|------|--------|
| **フロントエンド ファイル数** | 517 | 350-400 | -23% ~ -32% |
| **components/ui/ ファイル数** | 158 | 30 | -81% |
| **stores/ ファイル数** | 52 | 20-25 | -52% ~ -62% |
| **services/domain/ ファイル数** | 56 | 0 (削除) | -100% |
| **バックエンド クレート数** | 9 | 10 | +1 (分離) |
| **複雑さ (全体)** | 100% | 60-70% | -30% ~ -40% |

### 定性的効果

1. **保守性の向上**
   - ファイル数削減により、修正箇所の特定が容易
   - 責務が明確化され、バグの混入リスク低減

2. **オンボーディングの改善**
   - 新規開発者が構造を理解しやすい
   - ドキュメントの充実

3. **拡張性の向上**
   - Aggregate Pattern により、関連機能の追加が容易
   - レイヤリングの明確化

4. **テスト容易性の向上**
   - 責務が明確化され、単体テストが書きやすい
   - モックの作成が容易

---

## ⚠️ リスクと注意事項

### リスク 1: 大規模リファクタリングの影響

**リスク**:
- フェーズ1のタスク1.2（stores/とservices/domain/の統合）は影響範囲が広い
- 既存機能の破壊リスク

**軽減策**:
- 機能ごとに段階的に実施（Task → Project → Tag → ...）
- 各段階でE2Eテストを実行
- フィーチャーフラグで新旧実装を切り替え可能に

### リスク 2: 開発速度の低下

**リスク**:
- リファクタリング期間中は新機能開発が停滞

**軽減策**:
- フェーズ1を1-2週間に集中して実施
- その後は通常開発と並行

### リスク 3: チーム内の認識齟齬

**リスク**:
- リファクタリングの目的や方針が共有されていない

**軽減策**:
- このドキュメントをチーム全体で共有
- 定期的なレビュー会議

---

## 📝 次のアクション

### 即座に実施すべきこと

1. ✅ このドキュメントをチームで共有・レビュー
2. ⬜ フェーズ1の実施スケジュール策定
3. ⬜ ストア設計ドキュメントの作成開始（タスク1.3）

### 検討が必要なこと

1. ⬜ stores/ vs services/domain/ の統合方針（オプションA or B）
2. ⬜ リファクタリング中の新機能開発方針
3. ⬜ E2Eテストの整備状況確認

### 長期的に検討すべきこと

1. ⬜ 型定義の自動生成の導入検討
2. ⬜ モノレポ化の検討（バックエンド・フロントエンド統合）
3. ⬜ CI/CDパイプラインでのアーキテクチャ検証自動化

---

## 📚 参考資料

- [Clean Architecture](https://blog.cleancoder.com/uncle-bob/2012/08/13/the-clean-architecture.html)
- [DDD Aggregates](https://martinfowler.com/bliki/DDD_Aggregate.html)
- [Facade Pattern](https://refactoring.guru/design-patterns/facade)
- [Svelte 5 Runes](https://svelte-5-preview.vercel.app/docs/runes)

---

## 変更履歴

| 日付 | 変更内容 | 担当者 |
|-----|---------|--------|
| 2025-11-23 | 初版作成（アーキテクチャ分析結果） | Claude Code |
